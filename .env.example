# Copy to .env and fill in. .env is gitignored.
# Gemini Flash via byLLM (Jac: gemini/gemini-2.5-flash).
# Optional: absolute path to bun if client UI 404s (jac needs it to bundle).
# JAC_BUN=/Users/you/.nvm/versions/node/v22.20.0/bin/bun
GOOGLE_API_KEY=
